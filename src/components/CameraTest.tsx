const CameraTest = () => {
  const handleStartCamera = constraints => {
    let stream = null;
  };

  return (
    <div>
      <button
        type="button"
        className="text-4xl font-bold mb-4 cursor-pointer bg-gray-100 py-2 px-4 rounded-md"
      >
        Start Camera
      </button>
    </div>
  );
};

export default CameraTest;
